#include <stdio.h>

int main(int argc, char **argv)
{
    int i = 0;
    int j = 0;
    int *ints;
    int spc = 0;

    while (argv[i])
    {
        while (argv[i][j])
        {
            if (argv[i][j] == ' ' )
            if (argv[i][j] == ' ')
                spc++;
            
        }
        
    }
    
}